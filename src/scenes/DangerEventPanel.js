/* ── SVG character art (articulated groups for CSS animation) ──── */

const SVG_TREXO = `<svg viewBox="0 0 60 54" fill="none" xmlns="http://www.w3.org/2000/svg" style="height:100%;width:auto;display:block">
<g class="dep-t-tail">
  <path d="M8 29 Q3 21 5 13 Q9 8 13 16 Q10 22 10 28" fill="#1e8448"/>
  <path d="M8 29 Q4 22 6 14 Q9 10 12 17" stroke="#28a85f" stroke-width="4" fill="none" stroke-linecap="round"/>
  <path d="M9 26 Q5 20 7 14" stroke="#3dc97a" stroke-width="2" fill="none" stroke-linecap="round" opacity="0.6"/>
</g>
<g class="dep-t-lr">
  <path d="M17 40 Q13 48 15 52" stroke="#186a3e" stroke-width="6.5" fill="none" stroke-linecap="round"/>
  <path d="M13 52 Q10 54 8 53 M15 52 L16 55 M18 52 L21 54" stroke="#186a3e" stroke-width="2.5" fill="none" stroke-linecap="round"/>
</g>
<ellipse cx="26" cy="36" rx="14" ry="5" fill="#156636" opacity="0.55"/>
<ellipse cx="25" cy="30" rx="16" ry="13" fill="#1e8448"/>
<ellipse cx="24" cy="29" rx="14" ry="11" fill="#28a85f"/>
<ellipse cx="21" cy="24" rx="9.5" ry="6" fill="#3dc97a" opacity="0.65"/>
<ellipse cx="19" cy="22" rx="5.5" ry="3.2" fill="rgba(130,255,165,0.28)"/>
<path d="M11 22 Q13.5 14 16 22" fill="#48e88a" stroke="#2aab60" stroke-width="0.6"/>
<path d="M17 18 Q19.5 10 22 18" fill="#48e88a" stroke="#2aab60" stroke-width="0.6"/>
<path d="M23 16 Q25.5 8 28 16" fill="#48e88a" stroke="#2aab60" stroke-width="0.6"/>
<path d="M33 27 Q30 33 33 36" stroke="#186a3e" stroke-width="4.5" fill="none" stroke-linecap="round"/>
<path d="M33 36 L29 39 M33 36 L36 40" stroke="#186a3e" stroke-width="2.5" fill="none" stroke-linecap="round"/>
<g class="dep-t-lf">
  <path d="M24 40 Q27 46 32 49" stroke="#1d8048" stroke-width="7" fill="none" stroke-linecap="round"/>
  <path d="M30 49 Q27 52 25 51 M32 49 L33 53 M35 49 L38 52" stroke="#186a3e" stroke-width="2.5" fill="none" stroke-linecap="round"/>
</g>
<g class="dep-t-head">
  <path d="M34 24 Q39 16 43 13" stroke="#1e8448" stroke-width="13" fill="none" stroke-linecap="round"/>
  <path d="M34 24 Q39 16 43 13" stroke="#28a85f"  stroke-width="10" fill="none" stroke-linecap="round"/>
  <path d="M34 24 Q39 16 43 13" stroke="#3dc97a"  stroke-width="6"  fill="none" stroke-linecap="round" opacity="0.55"/>
  <ellipse cx="47" cy="13.5" rx="11" ry="9.5" fill="#1e8448"/>
  <ellipse cx="47" cy="12.5" rx="10" ry="8.5"  fill="#28a85f"/>
  <ellipse cx="44" cy="9.5"  rx="7"  ry="4.2"  fill="#3dc97a" opacity="0.6"/>
  <ellipse cx="43" cy="8.5"  rx="4"  ry="2.2"  fill="rgba(155,255,185,0.27)"/>
  <path d="M53 15 Q59 18 57 23 Q52 25 49 22" fill="#1e8448"/>
  <path d="M53 15 Q58 18 56 22 Q52 24 50 22" fill="#28a85f"/>
  <ellipse cx="55" cy="16.5" rx="1.3" ry="1" fill="#156636"/>
  <circle cx="48.5" cy="11"   r="5.8" fill="white"/>
  <circle cx="48.5" cy="11"   r="5.8" fill="rgba(200,245,220,0.22)"/>
  <circle cx="50"   cy="11.5" r="4"   fill="#0e2018"/>
  <circle cx="50"   cy="11.5" r="3"   fill="#152c1e"/>
  <circle cx="50"   cy="11.5" r="2"   fill="#06100a"/>
  <circle cx="51.6" cy="9.8"  r="1.6" fill="white"/>
  <circle cx="49.3" cy="13"   r="0.7" fill="rgba(255,255,255,0.55)"/>
  <circle cx="48.5" cy="11"   r="5.8" stroke="#156636" stroke-width="0.8" fill="none"/>
  <path d="M44 6.8 Q48.5 4 52.5 6.8" stroke="#0a160e" stroke-width="2.2" fill="none" stroke-linecap="round"/>
  <path d="M50 21 Q54 25 58 21.5" stroke="#0a160e" stroke-width="1.6" fill="none" stroke-linecap="round"/>
  <line x1="52"  y1="21.5" x2="52.3" y2="25"   stroke="white" stroke-width="1.5" stroke-linecap="round"/>
  <line x1="55"  y1="22"   x2="55.2" y2="25.5" stroke="white" stroke-width="1.5" stroke-linecap="round"/>
</g>
</svg>`;

const SVG_SHARK = `<svg viewBox="0 0 72 46" fill="none" xmlns="http://www.w3.org/2000/svg" style="height:100%;width:auto;display:block">
<g class="dep-s-tail">
  <path d="M5 26 Q0 14 1 7 Q5 5 7 10 Q9 17 8 24" fill="#162f5a"/>
  <path d="M5 26 Q0 38 1 45 Q5 47 7 42 Q9 35 8 28" fill="#162f5a"/>
  <path d="M6 25 Q2 15 3 9 Q5 7 7 11" stroke="#2d5090" stroke-width="1.5" fill="none" stroke-linecap="round" opacity="0.5"/>
</g>
<ellipse cx="34" cy="28" rx="29" ry="14" fill="#142c56" opacity="0.5"/>
<ellipse cx="33" cy="26" rx="28" ry="13" fill="#243f70"/>
<ellipse cx="29" cy="20" rx="20" ry="7"  fill="#2d5090" opacity="0.7"/>
<ellipse cx="24" cy="17" rx="14" ry="4.5" fill="rgba(80,140,255,0.2)"/>
<ellipse cx="33" cy="31" rx="23" ry="9"  fill="#e2eef8"/>
<ellipse cx="30" cy="31" rx="17" ry="6.5" fill="rgba(255,255,255,0.45)"/>
<path d="M26 13 Q29 4 34 0 Q38 3 39 13" fill="#162f5a"/>
<path d="M27 13 Q30 5 34 1" stroke="#2d5090" stroke-width="1.5" fill="none" stroke-linecap="round" opacity="0.55"/>
<path d="M45 28 Q56 37 50 27 Z" fill="#162f5a"/>
<ellipse cx="55" cy="25" rx="14" ry="13" fill="#243f70"/>
<path d="M55 13 Q70 19 70 25 Q70 31 55 37" fill="#243f70"/>
<ellipse cx="58" cy="19" rx="8" ry="3.5" fill="rgba(80,140,255,0.16)"/>
<path d="M42 18 Q41 25 42 32" stroke="#162f5a" stroke-width="1.8" fill="none" stroke-linecap="round"/>
<path d="M38 17 Q37 25 38 33" stroke="#162f5a" stroke-width="1.8" fill="none" stroke-linecap="round"/>
<path d="M34 16 Q33 25 34 34" stroke="#162f5a" stroke-width="1.5" fill="none" stroke-linecap="round"/>
<circle cx="60" cy="21" r="4"   fill="white"/>
<circle cx="60" cy="21" r="4"   fill="rgba(0,0,30,0.18)"/>
<circle cx="61" cy="21.5" r="2.8" fill="#04040a"/>
<circle cx="62.2" cy="20.2" r="1" fill="white"/>
<path d="M56.5 17.5 Q59.5 16 63 18" stroke="#08081a" stroke-width="2.2" fill="none" stroke-linecap="round"/>
<path d="M57 31 Q63 36 69 32 L69 37 Q63 42 57 37 Z" fill="#080c18"/>
<path d="M57 31 Q63 36 69 32" stroke="#101828" stroke-width="1.8" fill="none" stroke-linecap="round"/>
<g class="dep-s-jaw">
  <path d="M57 37 Q63 42 69 37" stroke="#101828" stroke-width="1.5" fill="none" stroke-linecap="round"/>
  <line x1="59"   y1="33" x2="59.2" y2="37.5" stroke="white" stroke-width="1.7" stroke-linecap="round"/>
  <line x1="62"   y1="35" x2="62.2" y2="39.5" stroke="white" stroke-width="1.7" stroke-linecap="round"/>
  <line x1="65"   y1="35" x2="65.2" y2="39.5" stroke="white" stroke-width="1.7" stroke-linecap="round"/>
  <line x1="67.5" y1="34" x2="67.5" y2="38"   stroke="white" stroke-width="1.4" stroke-linecap="round"/>
</g>
</svg>`;

/* ── Messages ─────────────────────────────────────────────────── */

const MSGS = {
  match:   ['¡Sigue así!', '¡Eso me ayuda!', '¡Bien hecho!', '¡Un poco más!'],
  booster: ['¡Eso me ayudó mucho!', '¡Qué potencia!', '¡Impulso brutal!'],
  danger:  ['¡Rápido, se acerca!', '¡Cuidado, apura!', '¡No te pares!'],
  win:     '¡Me salvaste! 🎉',
  lose:    '¡El tiburón me alcanzó…',
};
const pick = src => Array.isArray(src) ? src[Math.floor(Math.random() * src.length)] : src;

/* ── Panel class ──────────────────────────────────────────────── */

export class DangerEventPanel {
  constructor() {
    this.element = null;
    this._dist   = 40;
  }

  createIn(container) {
    this.element = document.createElement('div');
    this.element.className = 'dep';
    this.element.innerHTML = `
      <div class="dep-scene" id="dep-scene">
        <div class="dep-sky"></div>
        <div class="dep-sun"></div>
        <div class="dep-ocean">
          <div class="dep-wl dep-wl1"></div>
          <div class="dep-wl dep-wl2"></div>
          <div class="dep-wl dep-wl3"></div>
          <div class="dep-wl dep-wl4"></div>
        </div>
        <div class="dep-shore"></div>
        <div class="dep-foam dep-fa"></div>
        <div class="dep-foam dep-fb"></div>
        <div class="dep-foam dep-fc"></div>
        <div class="dep-foam dep-fd"></div>
        <div class="dep-particles" id="dep-particles"></div>
        <div class="dep-overlay" id="dep-overlay"></div>
        <div class="dep-shark-wrap" id="dep-shark">
          <div class="dep-shark-wake"></div>
          ${SVG_SHARK}
        </div>
        <div class="dep-trexo-wrap" id="dep-trexo">
          <div class="dep-wake"></div>
          <div class="dep-trexo-glow" id="dep-trexo-glow"></div>
          ${SVG_TREXO}
        </div>
        <div class="dep-msg" id="dep-msg">¡El tiburón me sigue!</div>
      </div>
      <div class="dep-bar-wrap">
        <div class="dep-bar-fill" id="dep-bar"></div>
      </div>
    `;
    container.appendChild(this.element);
    this._apply(false);
  }

  onMatch()    { this._move(+8,   pick(MSGS.match),   'match');   }
  onBooster()  { this._move(+18,  pick(MSGS.booster), 'booster'); }
  onLowMoves() { this._move(-12,  pick(MSGS.danger),  false);     }
  onWin()      { this._moveTo(100, MSGS.win,  'win');  }
  onLose()     { this._moveTo(0,   MSGS.lose, 'lose'); }

  onRocket()    { this._move(+22, '¡Turbo! 🚀',              'booster'); }
  onBomb()      { this._bombWave();        this._move(+15, '¡Ola explosiva! 💥', 'booster'); }
  onColorBomb() { this._rainbowGlow();     this._move(+18, '¡Energía arcoíris! 🌈', 'booster'); }
  onPtero()     { this._spawnPteroHelper(); this._move(+14, '¡Ayuda aérea! 🦅', 'match'); }
  onMegaCombo() { this._bombWave(); this._flashScene(); this._move(+30, '¡Combo salvaje! 🔥', 'booster'); }

  onTimePressure(timePct) {
    if (!this.element) return;
    // Shark creeps closer as time runs out
    if (timePct < 0.5) {
      const delta = timePct < 0.17 ? -2.2 : timePct < 0.33 ? -1.0 : -0.45;
      this._dist  = Math.max(0, this._dist + delta);
      this._apply(false);
    }
    // Danger overlay follows time pressure
    const overlay = this.element.querySelector('#dep-overlay');
    const scene   = this.element.querySelector('#dep-scene');
    if (!overlay?.classList.contains('dep-overlay--win')) {
      overlay?.classList.toggle('dep-overlay--danger', timePct < 0.30 || this._dist < 30);
      scene?.classList.toggle('dep-scene--tense', timePct < 0.17);
    }
  }

  setMessage(msg) { this._setMsg(msg); }

  _move(delta, msg, fx) {
    this._dist = Math.max(0, Math.min(100, this._dist + delta));
    this._apply(true, fx);
    this._setMsg(msg);
  }

  _moveTo(target, msg, state) {
    this._dist = target;
    this._apply(true, state === 'win' ? 'booster' : false);
    this._setMsg(msg);
    this._setOverlay(state);
  }

  _apply(animate, fx = false) {
    if (!this.element) return;
    const shark   = this.element.querySelector('#dep-shark');
    const trexo   = this.element.querySelector('#dep-trexo');
    const bar     = this.element.querySelector('#dep-bar');
    const scene   = this.element.querySelector('#dep-scene');
    const overlay = this.element.querySelector('#dep-overlay');
    const d       = this._dist;

    if (shark) {
      shark.style.left = `${2 + (100 - d) * 0.30}%`;
      shark.classList.toggle('dep-shark--close', d < 30);
    }
    if (trexo) {
      trexo.style.left = `${50 + d * 0.13}%`;
      if (animate) {
        const burst = fx === 'booster' ? 'dep-trexo--boost' : 'dep-trexo--burst';
        trexo.classList.add(burst);
        setTimeout(() => trexo?.classList.remove(burst), fx === 'booster' ? 480 : 380);
      }
    }
    if (bar) bar.style.width = `${d}%`;
    if (overlay && !overlay.classList.contains('dep-overlay--win'))
      overlay.classList.toggle('dep-overlay--danger', d < 30);
    if (scene) {
      scene.classList.toggle('dep-scene--tense', d < 20);
    }

    if (fx === 'booster') { this._spawnParticles(); this._spawnSpeedLines(1); }
    else if (fx === 'match') this._spawnSpeedLines(0.35);
  }

  _setOverlay(state) {
    const el    = this.element?.querySelector('#dep-overlay');
    const scene = this.element?.querySelector('#dep-scene');
    if (!el) return;
    el.classList.remove('dep-overlay--danger', 'dep-overlay--win');
    if (scene) { scene.classList.remove('dep-scene--tense'); scene.classList.toggle('dep-scene--win', state === 'win'); }
    if (state) el.classList.add(`dep-overlay--${state}`);
  }

  _spawnParticles() {
    const host    = this.element?.querySelector('#dep-particles');
    const trexoEl = this.element?.querySelector('#dep-trexo');
    if (!host || !trexoEl) return;
    const bx = parseFloat(trexoEl.style.left || '55');
    for (let i = 0; i < 7; i++) {
      const p = document.createElement('div');
      p.className = 'dep-particle';
      const a = -Math.PI * 0.5 + (Math.random() - 0.5) * Math.PI * 0.85;
      const r = 14 + Math.random() * 26;
      p.style.left   = `${(bx + (Math.random() - 0.5) * 5).toFixed(1)}%`;
      p.style.bottom = `${(12 + Math.random() * 22).toFixed(0)}px`;
      p.style.animationDelay = `${Math.floor(Math.random() * 90)}ms`;
      p.style.setProperty('--tx', `${Math.round(Math.cos(a) * r)}px`);
      p.style.setProperty('--ty', `${Math.round(Math.sin(a) * r)}px`);
      host.appendChild(p);
      setTimeout(() => p.remove(), 700);
    }
  }

  _spawnSpeedLines(intensity) {
    const host    = this.element?.querySelector('#dep-particles');
    const trexoEl = this.element?.querySelector('#dep-trexo');
    if (!host || !trexoEl) return;
    const bx    = parseFloat(trexoEl.style.left || '55');
    const count = Math.round(2 + intensity * 3);
    for (let i = 0; i < count; i++) {
      const sl = document.createElement('div');
      sl.className = 'dep-speedline';
      sl.style.top  = `${(25 + Math.random() * 45).toFixed(0)}%`;
      sl.style.left = `${(bx - 6 - Math.random() * 14).toFixed(1)}%`;
      sl.style.setProperty('--sl-len', `${Math.round(16 + Math.random() * 28 * intensity)}px`);
      sl.style.animationDelay = `${Math.floor(Math.random() * 80)}ms`;
      host.appendChild(sl);
      setTimeout(() => sl.remove(), 430);
    }
  }

  _bombWave() {
    const shark = this.element?.querySelector('#dep-shark');
    if (!shark) return;
    shark.classList.add('dep-shark--wave');
    setTimeout(() => shark?.classList.remove('dep-shark--wave'), 700);
  }

  _rainbowGlow() {
    const glow = this.element?.querySelector('#dep-trexo-glow');
    if (!glow) return;
    glow.classList.remove('dep-glow--active');
    void glow.offsetWidth;
    glow.classList.add('dep-glow--active');
    setTimeout(() => glow?.classList.remove('dep-glow--active'), 700);
  }

  _spawnPteroHelper() {
    const host = this.element?.querySelector('#dep-particles');
    if (!host) return;
    const el = document.createElement('div');
    el.className = 'dep-ptero-helper';
    el.textContent = '🦅';
    host.appendChild(el);
    setTimeout(() => el.remove(), 860);
  }

  _flashScene() {
    const overlay = this.element?.querySelector('#dep-overlay');
    if (!overlay) return;
    overlay.classList.add('dep-overlay--flash');
    setTimeout(() => overlay?.classList.remove('dep-overlay--flash'), 400);
  }

  _setMsg(msg) {
    const el = this.element?.querySelector('#dep-msg');
    if (!el) return;
    el.textContent = msg;
    el.classList.remove('dep-msg--pop');
    void el.offsetWidth;
    el.classList.add('dep-msg--pop');
    setTimeout(() => el?.classList.remove('dep-msg--pop'), 400);
  }

  destroy() {
    this.element?.remove();
    this.element = null;
  }
}
